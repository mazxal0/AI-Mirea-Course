# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):
- `S07-hw-dataset-01`
- `S07-hw-dataset-02`
- `S07-hw-dataset-04`

### 1.1 Dataset A

- Файл: `S07-hw-dataset-01`
- Размер: (12000, 9)
- Признаки: все признаки числовые, тип float64 (кроме sample_id, он int64)
- Пропуски: нет
- "Подлости" датасета: разные шкалы и немного выбросов

### 1.2 Dataset B

- Файл: `S07-hw-dataset-02`
- Размер: (8000, 4)
- Признаки: все признаки числовые, тип float64 (кроме sample_id, он int64)
- Пропуски: нет
- "Подлости" датасета: достаточно выбросов

### 1.3 Dataset C

- Файл: `S07-hw-dataset-04`
- Размер: (10000, 33)
- Признаки: есть признаки и числовые типа float64 и категориальные (`cat_a` и `cat_b`) (кроме sample_id, он int64)
- Пропуски: нет
- "Подлости" датасета: разные шкалы и выбросы и разная плотность и категориальные

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- Препроцессинг: 
  - scaling для приведения признаков к пространству, где значения имеют среднее равное 0 и std = 1, 
  - imputation помогало нам заменить пропуски, в числовых, 
  - encoding, 
  - PCA – показывало, как при сжатии до двух-признакового пространства примерное взаимоотношение объектов 
- Поиск гиперпараметров:
  - для KMeans: k перебирал по `[2, ..., 20]`
  - для DBScan: eps по `[0.1, 0.2, ..., 3.0]` и min_samples по `[3, 5, 10]`
  - linkage по `['ward', 'average']`
  - выбирал лучшее по silhouette и по тому, как разделяли модели объекты, какие выборки получились, какой шум у DBScan
- Метрики: silhouette / Davies-Bouldin / Calinski-Harabasz для всех моделей, если у DBScan при разделение получалось больше двух кластеров и общее число объектов, не являющихся шумом, больше 1, то считал эти метрики, иначе np.nan
- Визуализация: PCA(2D) помогали примерно посмотреть как разделялись объекты на кластеры с сохранением глобальной структуры, а t-SNE помогало посмотреть на локальный уровень кластеров, которые показывают взаимоотношение подгрупп внутри кластеров 

## 3. Models

Перечислите, какие модели сравнивали **на каждом датасете**, и какие параметры подбирали.

Минимум (для каждого датасета):

### Dataset A
- KMeans (поиск `k`, фиксировали `random_state=42`, `n_init=10`)
- DBSCAN (`eps`, `min_samples`, доля шума)

### Dataset B
- KMeans (поиск `k`, фиксировали `random_state=42`, `n_init=10`)
- DBSCAN (`eps`, `min_samples`, доля шума)

### Dataset C
- KMeans (поиск `k`, фиксировали `random_state=42`, `n_init=10`)
- DBSCAN (`eps`, `min_samples`, доля шума), или
- AgglomerativeClustering (`k`, `linkage`)

## 4. Results

Для каждого датасета – краткая сводка результатов.

### 4.1 Dataset A

- Лучший метод KMeans(k=6):
- Метрики `silhouette=0.580`, `DB=0.568`, `CH=15669`:
- Хорошо делит на 2 видимых по PCA кластера

### 4.2 Dataset B

- Лучший метод KMeans(k=6):
- Метрики `silhouette=0.306`, `DB=1.32`, `CH=3573`:
- Хорошо делит на 2 видимых по PCA кластера, 

### 4.3 Dataset C

- DBScan(eps=2.1, min_samples=10):
- Метрики `silhouette=0.636`, `DB=1.55`, `CH=14645`:
- Если был DBSCAN: `noise_share=0.0024`
- Довольно хорошо работает с большим кол-вом признаков, поэтому легче находить 6 кластера

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- При неправильном подборе k может сломаться, при вытянутости и разных масштабах данных
- При большом признаковом пространстве, есть выбросы или шумы, если кластеры произвольной формы?
- Масштабирование, шумы и плотность?

### 5.2 Устойчивость (обязательно для одного датасета)

Делал 5 запусков KMeans по разным seed для dataset A и получил 
`ARI scores: [1.0, 1.0, 1.0, 1.0]
Mean ARI: 1.0`, что показывает нам, что random_state никак не повлиял на распределение по кластерам и они получились 
идентичны, что даёт нам доказательство сильной устойчивости и независимость от random_state

### 5.3 Интерпретация кластеров

- Как вы интерпретировали кластеры:
  Интерпретация кластеров выполнялась преимущественно на основе визуализации в пространстве главных компонент (PCA), поскольку данные являются синтетическими и не имеют бизнес-семантики. PCA позволила оценить взаимное расположение кластеров, их разделимость и компактность.
  Также объекты, которые на графике выглядят отстранённо скорей всего являются шумом и/или выбросами.
  В целом данные довольно хорошо делятся во всех датасетах однозначно

## 6. Conclusion

В данной работе я научился работать с алгоритмами кластеризации KMeans, DBSCAN и AgglomerativeClustering, а также подбирать и анализировать их гиперпараметры. В ходе выполнения работы был получен опыт обработки шума, заполнения и устранения пропусков, а также кодирования категориальных признаков с использованием OneHotEncoder.

Кроме того, я научился выбирать наиболее подходящие модели на основе различных метрик качества и анализировать ситуации, в которых отдельные алгоритмы демонстрируют аномально высокие значения метрик по сравнению с другими методами.
